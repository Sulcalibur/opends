{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "./schema.json",
  "title": "Ember design-system dataset",
  "description": "JSON Schema for the Ember demo design system shipped in design-system-data/. tokens.json references this file via its $schema field.",
  "type": "object",
  "properties": {
    "$schema": { "type": "string" },
    "description": { "type": "string" },
    "tokens": {
      "type": "array",
      "description": "One entry per token. value may be any JSON value; category uses the OpenDS admin picker values.",
      "items": {
        "type": "object",
        "required": ["name", "value", "category"],
        "properties": {
          "name": { "type": "string", "minLength": 1 },
          "value": { "description": "Any JSON value: string (hex/px), number, or object for composite tokens" },
          "category": {
            "type": "string",
            "enum": ["color", "font", "space", "radius", "shadow", "motion", "z-index"]
          },
          "description": { "type": "string" }
        }
      }
    }
  },
  "required": ["tokens"]
}
