# Getting Started

## Install Dependencies
```bash
npm i
```

## Run Dev Servers
```bash
npm run dev
```

## Configure Environment
Create `.env` files from the provided `.env.example` in both `backend/` and `frontend/`.

